FastAPI :

 Pydantic is a Python library for data validation. Its whole job is making sure data is the right shape and type before your code uses it. 
 used with FastAPI
 ---                                                   
  Converting to/from JSON

  # Python object → JSON string
  person.model_dump_json()                                                                                                                                                        
  # '{"name": "Ash", "age": 22, "email": "ash@gmail.com"}'

  FastAPI is built on top of Pydantic

  JSON string

  js object notaitin, csv values
  
